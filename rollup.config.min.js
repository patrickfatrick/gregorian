import uglify from 'rollup-plugin-uglify'
import base from './rollup.config.base'

export default Object.assign(base, {
  dest: 'dist/gregorian.min.js',
  format: 'umd',
  moduleName: 'gregorian',
  plugins: base.plugins.concat(uglify())
})
