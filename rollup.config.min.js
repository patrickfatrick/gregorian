import { uglify } from 'rollup-plugin-uglify';
import base, { plugins as basePlugins } from './rollup.config.base';

const plugins = [...basePlugins, uglify()];

export default [
  {
    ...base[0],
    output: {
      file: 'dist/gregorian.min.js',
      format: 'umd',
      name: 'gregorian',
    },
    plugins,
  },
  {
    ...base[1],
    output: {
      file: 'dist/locale.min.js',
      format: 'umd',
      name: 'gregorianLocales',
    },
    plugins,
  },
];
